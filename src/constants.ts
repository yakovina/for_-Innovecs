export const DEFAULT_TIMEOUT = 30;
export const ADDED_ANGLE = 120;

export enum SLOT_NAMES_ENUM {
    SLOT1 = 'slot-1',
    SLOT2 = 'slot-2',
    SLOT3 = 'slot-3'
}

export enum SLOT_EVENTS_ENUM {
    START = ':start',
    STOP = ':stop',
}